# Firmware tests

Planned:

- ztest coverage for release metadata helpers,
- ztest coverage for loop telemetry calculations,
- QEMU smoke test that extracts jitter lines from serial output.

Current v0.1 host tests live in `tests/` and cover the Python release tooling.
